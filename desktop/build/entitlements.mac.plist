<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    The hardened runtime is required for notarisation, and it switches off the
    things Electron needs unless they are asked for back. These three are the
    minimum an Electron application runs with, and nothing beyond them is here:
    FileClear has no camera, no microphone, no Apple Events and no access to
    anybody's documents, so asking for those permissions would be asking for
    trust the app has no use for.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
